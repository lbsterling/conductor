%h2 Log in now

- form_for @user_session, :url => session_path do |f|
  = f.error_messages
  .login_alternative{hidden_unless(@user_session.openid_identifier.blank?)}
    = f.text_field :email
    = f.password_field :password
  .login_alternative{hidden_if(@user_session.openid_identifier.blank?)}
    = f.text_field :openid_identifier, :label => image_tag('openid.png', :alt => 'OpenID')
  = f.check_box :remember_me, :label => 'remember me'
  = f.submit "Log in"
%ul#hints
  %li.login_alternative{hidden_unless(@user_session.openid_identifier.blank?)}= link_to_function 'Log in using OpenID instead', "$('.login_alternative').toggle();"
  %li.login_alternative{hidden_unless(@user_session.openid_identifier.blank?)}= link_to 'Forgot your password?', new_password_path
  %li.login_alternative{hidden_if(@user_session.openid_identifier.blank?)}= link_to_function "Log in using a standard username/password", "$('.login_alternative').toggle();"
  %li= link_to('Sign up for an account now', new_user_path)
